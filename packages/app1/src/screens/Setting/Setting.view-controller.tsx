import {Text, View} from 'react-native';

interface SettingProps {}

const Setting = ({}: SettingProps) => {
  return (
    <View style={{flex: 1}}>
      <Text>Setting Screen Now!</Text>
    </View>
  );
};

export default Setting;
