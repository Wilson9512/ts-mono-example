{
  "name": "@ts-monorepo/common",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "postinstall": "yarn build",
    "watch": "tsc --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "jest": "^28.1.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.5",
    "typescript": "5.0.4"
  }
}
